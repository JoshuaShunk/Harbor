name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Harbor ${{ github.ref_name }}"
          body: "See the assets below to download Harbor for your platform."
          draft: false
          prerelease: false

  build:
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            arch: aarch64
          - platform: macos-latest
            target: x86_64-apple-darwin
            arch: x64

    runs-on: ${{ matrix.platform }}

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2

      - name: Install frontend dependencies
        working-directory: ui
        run: npm ci

      - name: Build frontend
        working-directory: ui
        run: npm run build

      - name: Install Tauri CLI
        run: npm install -g @tauri-apps/cli

      - name: Build Tauri app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          HARBOR_GITHUB_CLIENT_ID: ${{ secrets.HARBOR_GITHUB_CLIENT_ID }}
          HARBOR_GITHUB_CLIENT_SECRET: ${{ secrets.HARBOR_GITHUB_CLIENT_SECRET }}
          HARBOR_GOOGLE_CLIENT_ID: ${{ secrets.HARBOR_GOOGLE_CLIENT_ID }}
          HARBOR_GOOGLE_CLIENT_SECRET: ${{ secrets.HARBOR_GOOGLE_CLIENT_SECRET }}
          HARBOR_SLACK_CLIENT_ID: ${{ secrets.HARBOR_SLACK_CLIENT_ID }}
          HARBOR_SLACK_CLIENT_SECRET: ${{ secrets.HARBOR_SLACK_CLIENT_SECRET }}
        working-directory: crates/harbor-desktop
        run: npx tauri build --target ${{ matrix.target }}

      - name: Package and sign updater artifact
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: |
          BUNDLE_DIR="target/${{ matrix.target }}/release/bundle"
          APP_DIR="${BUNDLE_DIR}/macos/Harbor.app"
          TARGZ="${BUNDLE_DIR}/macos/Harbor_${{ matrix.arch }}.app.tar.gz"

          # Create tar.gz from .app bundle
          tar -czf "${TARGZ}" -C "${BUNDLE_DIR}/macos" Harbor.app

          # Sign the tar.gz with Tauri signer
          npx tauri signer sign "${TARGZ}" --private-key "$TAURI_SIGNING_PRIVATE_KEY" --password "${TAURI_SIGNING_PRIVATE_KEY_PASSWORD:-}"

      - name: Upload release assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BUNDLE_DIR="target/${{ matrix.target }}/release/bundle"

          # Upload DMG
          DMG=$(ls ${BUNDLE_DIR}/dmg/*.dmg)
          gh release upload ${{ github.ref_name }} "${DMG}" --repo ${{ github.repository }} --clobber

          # Upload signed .app.tar.gz
          TARGZ="${BUNDLE_DIR}/macos/Harbor_${{ matrix.arch }}.app.tar.gz"
          gh release upload ${{ github.ref_name }} "${TARGZ}" --repo ${{ github.repository }} --clobber

          # Upload signature
          SIG="${TARGZ}.sig"
          gh release upload ${{ github.ref_name }} "${SIG}" --repo ${{ github.repository }} --clobber

  publish-updater:
    needs: [create-release, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Build and upload latest.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Download signature files
          gh release download ${{ github.ref_name }} --pattern "*.sig" --repo ${{ github.repository }} --dir sigs

          AARCH64_SIG=$(cat sigs/Harbor_aarch64.app.tar.gz.sig)
          X64_SIG=$(cat sigs/Harbor_x64.app.tar.gz.sig)

          AARCH64_URL="https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/Harbor_aarch64.app.tar.gz"
          X64_URL="https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/Harbor_x64.app.tar.gz"

          VERSION="${{ github.ref_name }}"
          VERSION="${VERSION#v}"

          cat > latest.json << EOF
          {
            "version": "${VERSION}",
            "notes": "Harbor ${VERSION}",
            "pub_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "platforms": {
              "darwin-aarch64": {
                "signature": "${AARCH64_SIG}",
                "url": "${AARCH64_URL}"
              },
              "darwin-x86_64": {
                "signature": "${X64_SIG}",
                "url": "${X64_URL}"
              }
            }
          }
          EOF

          cat latest.json
          gh release upload ${{ github.ref_name }} latest.json --repo ${{ github.repository }} --clobber
